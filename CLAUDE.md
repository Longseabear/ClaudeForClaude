# CLAUDE.md

This project is **ClaudeForClaude (CLFC)**.

Read `AGENTS.md` first. It is the implementation contract for the repository:

- session model
- storage layout
- CLI behavior
- Claude Code and Ollama integration assumptions
- validation expectations

When working in this repository, follow `AGENTS.md` as the source of truth unless the user explicitly updates the contract.
